# TODO

## Swift版 高機能化タスク

- [x] Task 1: 3カラムレイアウトの土台を作る
  - 左カラムに `保存済み設定一覧` を配置する
  - 中央カラムに `基本設定` `文字選択エディタ` `生成ルール` を配置する
  - 右カラムに `生成結果` `強度評価` `点数` `警告` `偏りやユニーク率の可視化` を配置する
  - 左カラムの表示/非表示を切り替えられるようにする

- [x] Task 2: 右カラムの補助情報表示を追加する
  - 生成結果カードごとに補助情報を表示する形へ整理する
  - `Variety` を追加して、文字数と文字集合を踏まえた自然なばらつきを見られるようにする
  - `Balance` を追加して、文字の分布が偏りすぎていないか見られるようにする
  - 警告文を各結果ごとに表示する
  - この段階では既存ロジックと軽い補正を使って表示を整える

- [x] Task 3: 文字選択エディタをタブUIにする
  - `大文字` `小文字` `数字` `記号` のタブを追加する
  - タブごとに個別文字ボタン群を表示できるようにする
  - タブの外側に `現在選択中の文字` 表示を追加する
  - `現在選択中の文字` は全候補を表示し、選択中は太字、未選択は細字かつ薄い色にする

- [x] Task 4: 個別文字選択を生成ロジックへ反映する
  - 大文字、小文字、数字、記号ごとに選択された文字だけを生成対象にする
  - 既存のカテゴリ単位 ON/OFF と矛盾しない状態管理に整理する
  - 文字が1つも使えない場合の扱いをSwift版に合わせて整理する

- [x] Task 5: 生成ルールを拡張する
  - 文字種をなるべく均等にする設定を追加する
  - 先頭文字の制限を追加する
  - 同一文字の最大連続数を設定できるようにする
  - 使いたくない文字の除外設定を追加する

- [ ] Task 6: 保存済み設定一覧を実装する
  - 中央カラムの設定内容を名前付きで保存できるようにする
  - 保存済み設定を一覧表示する
  - 一覧から設定を呼び出せるようにする
  - 一覧から削除できるようにする
  - 一覧の並び替えができるようにする

- [ ] Task 7: 強度分析ロジックを強化する
  - 連番の減点を追加する
  - 繰り返しの減点を追加する
  - キーボード配列パターンの減点を追加する
  - 辞書語っぽさの警告を追加する
  - 点数と評価の関係が分かる表示にする
  - 減点理由を右カラムに表示する

- [ ] Task 8: アプリ設定画面を拡張する
  - アプリ全体テーマ設定を追加する
  - クリップボード自動クリアタイマー設定を追加する
  - グローバルショートカット設定を追加する
  - 生成画面の設定とアプリ全体設定の責務を分けて整理する
