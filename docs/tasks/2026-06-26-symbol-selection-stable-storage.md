## 背景

記号の個別選択を `Bool` 配列だけで保存すると、記号一覧の順序変更や途中追加で保存済み設定の意味がずれる。

## 対応

- [x] プリセット保存形式で、記号選択を順序依存の `symbols` ではなく選択された記号文字列として保存する。
- [x] 未リリースのため、旧 `symbols` 配列や旧 version 1 の読み込み互換は持たない。
- [x] export JSON の形式バージョンを更新する。
- [x] `docs/design/preset-saving.md` に記号選択保存の互換方針を追記する。
