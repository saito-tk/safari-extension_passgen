# リポジトリガイド

## プロジェクト概要
- このリポジトリは、macOS 向けのパスワードジェネレータアプリを扱います。
- 現在の実装はネイティブの SwiftUI 画面が中心です。
- メイン画面は 3 カラム構成で、設定・保存済み設定・結果を 1 ウィンドウ内で完結させてください。
- 将来的な常駐化やショートカット起動を見据え、アプリ本体の責務を明確に保ってください。

## 現在の UI 構成
- 左カラム
  - 保存済み設定一覧
- 中央カラム
  - 文字数 / 件数
  - 文字セット選択
  - 記号選択
  - 記号文字列の貼り付け反映
  - 生成ルール
  - 表示テーマ切り替え
- 右カラム
  - 生成結果一覧
  - 進捗表示 `(n/件数)`
  - 行ごとのコピーアイコン
  - 強度や補助情報

## プロジェクト構成
- `xcode/Passgen/Passgen/NativePasswordGeneratorView.swift`: メイン UI、生成ロジック、結果表示。
- `xcode/Passgen/Passgen/ViewController.swift`: メインウィンドウで SwiftUI 画面をホスト。
- `xcode/Passgen/Passgen/AppPreferences.swift`: アプリ全体の設定保持。
- `xcode/Passgen/Passgen/SettingsWindowController.swift`: 設定ウィンドウ。
- `xcode/Passgen/Passgen/Base.lproj/Main.storyboard`: アプリのメインウィンドウ。
- `README.md`: ユーザー向けのセットアップと仕様説明。
- `AGENTS.md`: Codex 向けの作業ガイド。

## プロダクト要件
- パスワード生成には暗号学的に安全な乱数を使います。
- 基本 UX はアプリ内で完結させてください。ユーザー要件がない限り、不要な常駐機能や外部アクセスは増やさないでください。
- UI 文言は、日本語対応の変更依頼がない限り日本語を維持してください。
- 設定保存は既存のアプリ設定との互換性を意識してください。
- 結果一覧では長いパスワードを先頭 100 文字まで省略表示しますが、コピー時は全文字列を使ってください。
- テーマは見た目のみの設定です。生成ロジックへ影響を与えないでください。

## 入力仕様
- 文字数
  - 最小 `4`
  - 最大 `999999`
- 件数
  - 最小 `1`
  - 最大 `1000`
  - ただし `min(1000, floor(9999990 / 文字数))` の動的上限を維持してください
- 数値入力は編集中に強制補正せず、フォーカスが外れた時点で補正してください。

## 記号選択の仕様
- `記号を含める` がオフの時は記号一覧と補助 UI を隠してください。
- 記号貼り付け欄に入力された文字列から、対応する記号だけを選択状態へ反映します。
- 対応外の文字だけが入力された場合は、記号選択状態を変えず、入力欄だけクリアしてください。
- `反映` ボタンは入力欄に何か入っている時だけ活性化してください。

## 生成中の仕様
- アプリ起動時点では自動生成しません。`生成` ボタン押下時のみ生成します。
- 生成中は設定系 UI を非活性にしてください。
- 生成中でも UI が固まりにくいよう、長い生成は分割して進めてください。
- 結果は逐次追加表示してください。

## テーマ仕様
- デフォルトは青テーマです。
- 少なくとも以下のテーマを維持してください。
  - 青
  - 緑
  - ピンク
  - 赤
  - 黄色
  - オレンジ
  - 紫
- テーマ選択 UI は中央カラム下部の控えめな見た目の独立セクションに置いてください。
- プルダウンではなく色スウォッチで選ぶ現行 UI を維持してください。
- 選択状態やフォーカス色に青固定値を残さず、テーマ変数へ追従させてください。

## コーディング指針
- 素の Swift / SwiftUI を優先し、小さく明示的な型や関数を使ってください。ユーザーの指示がない限りフレームワークは導入しません。
- 明確な理由がない限り、ファイルは ASCII ベースを維持してください。
- ネイティブ UI の構成は抽象化しすぎず、標準 API を優先してください。
- スタイル変更時は、現在の方向性であるガラス調パネル、軽い影、テーマ切り替え可能な色設計を崩さないでください。

## 確認手順
- `xcode/Passgen/Passgen.xcodeproj/project.pbxproj` を変更したら、参照切れやターゲット設定崩れがないことを再確認してください。
- 手動確認では最低限以下を確認してください。
- アプリが正常に開くこと。
- `生成` ボタン押下時のみ生成されること。
- 生成中は設定系 UI が非活性になり、見た目も disabled と分かること。
- 選択した文字種に応じてパスワードが生成されること。
- 件数の動的上限が文字数に応じて変化すること。
- 記号貼り付け機能が仕様どおり動くこと。
- コピーアイコンが動作し、該当行にだけ `Copied!` が出ること。
- アプリを閉じて開き直しても設定とテーマが保持されること。

## macOS アプリ向け注意点
- このリポジトリは macOS アプリのソースです。
- 明示的な依頼がない限り、派生ビルド成果物や一時生成物はコミットしないでください。

## 変更時のルール
- タスク範囲外のユーザー変更は巻き戻さないでください。
- セットアップ手順やユーザー向け挙動を変えた場合は `README.md` も更新してください。
- 仕様値やユーザー向け挙動を変更した場合は、実装を正として `README.md`、`docs/design/`、`docs/tasks/`、この `AGENTS.md` など関連ドキュメントの値や説明が乖離しないように同時更新してください。
- 新規ファイルを追加する場合も、明確な理由がない限りルート構成はシンプルに保ってください。
